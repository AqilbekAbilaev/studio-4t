# Pin this project to the public npm registry, regardless of any private
# registry configured globally (e.g. a work mirror). Keeps package-lock.json
# resolvable in CI and for outside contributors.
registry=https://registry.npmjs.org/
